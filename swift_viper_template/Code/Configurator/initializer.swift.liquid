{% include 'header' %}

import UIKit

class {{ prefix }}{{ module_info.name }}ModuleInitializer: NSObject {

    //Connect with object on storyboard
    @IBOutlet weak var {{ module_info.name | downcase }}View: {{ prefix }}{{ module_info.name }}View!

    override func awakeFromNib() {

        let configurator = {{ prefix }}{{ module_info.name }}ModuleConfigurator()
        configurator.configureModuleForViewInput(viewInput: {{ module_info.name | downcase }}View)
    }

}
