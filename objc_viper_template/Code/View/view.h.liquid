{% include 'header' %}

#import <UIKit/UIKit.h>
#import "{{ prefix }}{{ module_info.name }}ViewOutput.h"

@interface {{ prefix }}{{ module_info.name }}View : UIViewController

@property (nonatomic, weak) id<{{ prefix }}{{ module_info.name }}ViewOutput> output;

@end
