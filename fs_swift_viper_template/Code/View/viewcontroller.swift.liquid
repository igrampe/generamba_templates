{% include 'header' %}

import UIKit
import APLViperStackSwift

class {{ prefix }}{{ module_info.name }}ViewController: APLModuleViewController, {{ prefix }}{{module_info.name}}ViewInput {

    var output: {{ prefix }}{{ module_info.name }}ViewOutput!

    // MARK: Life cycle
    override func viewDidLoad() {
        super.viewDidLoad()
        output.viewIsReady()
    }


    // MARK: {{module_info.name}}ViewInput
    func setupInitialState() {
    }
}
