{% include 'header' %}

import APLViperStackSwift

class {{ prefix }}{{ module_info.name }}Router {
	
	var transitionHandler: APLTransitionHandler?
}

extension {{ prefix }}{{ module_info.name }}Router: {{ prefix }}{{ module_info.name }}RouterInput {
	
}
